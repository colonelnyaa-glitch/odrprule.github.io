(()=>{const list=document.querySelector('#historyList'),cat=document.querySelector('#categoryFilter'),order=document.querySelector('#orderFilter');let rows=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(){let data=rows.filter(x=>!cat.value||x.category===cat.value).sort((a,b)=>order.value==='old'?new Date(a.date)-new Date(b.date):new Date(b.date)-new Date(a.date));list.innerHTML=data.length?data.map(x=>`<article class="history-card"><div class="history-card-head"><div><time>${new Date(x.date).toLocaleString('ja-JP')}</time><h2>${esc(x.rule)}</h2></div><span>${esc(x.version||'')}</span></div><p class="meta">カテゴリー：${esc(x.category)}</p><section class="reason"><h3>変更後</h3><p>${esc(x.after||'記録なし')}</p></section><section class="reason"><h3>コミット内容</h3><p>${esc(x.commitMessage||'記録なし')}</p></section></article>`).join(''):'<p>変更履歴はまだありません。</p>'}fetch('data/changelog.json?'+Date.now()).then(r=>r.json()).then(d=>{rows=Array.isArray(d)?d:[];[...new Set(rows.map(x=>x.category).filter(Boolean))].sort().forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent=v;cat.append(o)});render()}).catch(()=>list.innerHTML='<p>変更履歴を読み込めませんでした。</p>');cat.addEventListener('change',render);order.addEventListener('change',render)})();